<h1>文字ジャンル詳細</h1>

<h5>文字ジャンル：<%= @genre.name %></h5>
<%= @letters.each do |letter| %>
   <li><%= link_to new_sample_practice_path(letter.id) do %>
           <%= letter.name %>
       <% end %></li>
<% end %>