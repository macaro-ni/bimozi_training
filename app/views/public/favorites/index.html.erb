<h1 class="m-3">お気に入り</h1>

<p>繰り返し練習したい文字をお気に入りに追加しておくと便利です。<br>
ハートボタンをタッチするとお気に入りから削除されます。</p>

<% @favorites.each do |favorite| %>
  <% if favorite.user_id == current_user.id %>
    <li class="list-unstyled">
        <%= link_to new_sample_practice_path(favorite.letter.id), data: {"turbolinks"=>false}, class: "border-bottom", style: "color: gray;" do %>
                     <%= favorite.letter.name %>
                     <!--お気に入り機能-->
                     <% if current_user.favorite_find(favorite.letter.id) %>
                        <%= link_to destroy_favorite_path(favorite.letter.id), method: :delete do %>
                          <i class="fa-solid fa-heart" style="color: #ecacac;"></i>
                        <% end %>
                      <% else %>
                        <%= link_to create_favorite_path(favorite.letter.id), method: :post do %>
                          <i class="fa-regular fa-heart" style="color: #ecacac;"></i>
                        <% end %>
                      <% end %>
                <% end %>
    </li>
  <% end %>
<% end %>